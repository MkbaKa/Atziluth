// 服务器完全启动后触发
function onEnable() {
    // info("enable!")
}

// 当前js被加载或重载时触发的函数
function onLoad() {
    // info("load!")
}

// 插件卸载时触发  通常只有服务器关闭的时候才会触发
function onDisable() {
    // info("disable")
}